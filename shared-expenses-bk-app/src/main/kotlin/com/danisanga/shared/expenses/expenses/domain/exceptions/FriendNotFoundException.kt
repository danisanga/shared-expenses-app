package com.danisanga.shared.expenses.expenses.domain.exceptions

/**
 * Custom exception for Friend domain (Not found friend).
 */
class FriendNotFoundException(message: String) : FriendException(message)