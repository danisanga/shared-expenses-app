package com.danisanga.shared.expenses.expenses.domain.exceptions

/**
 * Custom exception for Party domain (Not found party).
 */
class PartyNotFoundException(message: String) : PartyException(message)